function esPalindroma(palindroma){
  var palindroma2;
  for(var i=palindroma.length-1;i>=0;i--){
    palindroma2 += palindroma.charAt(i);
  }
  if(palindroma.equals(palindroma2))
  {
    //es palindroma
  }

}
